name: uplive-frontend

services:
  frontend:
    build:
      context: ../..
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - uplive_net
